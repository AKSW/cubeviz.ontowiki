// Typing for the phpjs.js

interface strpos { (haystack:string, needle:string, offset:number): number; }
declare var strpos: strpos;

interface implode { (separator:string, array:any): string; }
declare var implode: implode;
